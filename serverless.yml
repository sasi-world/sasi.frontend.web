service: sasi-frontend-web

#frameworkVersion: "1.35.1"

provider:
  name: aws
  runtime: nodejs12.x
  stage: dev
  region: eu-west-1
  environment:
    SLS_DEBUG: true

package:
  exclude:
    - ./**
myNextApplication:
  component: "@sls-next/serverless-component@{version_here}"

resources:
  - ${file(assets-pmbplus.yml)}
